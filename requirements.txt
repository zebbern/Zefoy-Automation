# Zefoy-CLI Automation Dependencies
# Install with: pip install -r requirements.txt

# Core - Browser automation
playwright>=1.40.0

# OCR for CAPTCHA solving
easyocr>=1.7.0
numpy>=1.24.0

# Image processing
Pillow>=10.0.0

# Spell correction for OCR improvement
pyenchant>=3.2.0

# Cryptography for fingerprint spoofing
pycryptodome>=3.20.0

# TUI (Terminal User Interface)
# Note: Using 0.89.1 for stability (7.x has breaking changes)
textual>=0.89.0,<1.0.0

# Development/Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0

requests